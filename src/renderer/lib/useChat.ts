// La logique de chat vit desormais dans le store (par conversation).
// On garde des re-exports de types pour compatibilite.
export type { UiMessage, UiToolEntry, UiToolEntry as ToolEntry } from '@shared/types'
